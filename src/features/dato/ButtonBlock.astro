---
interface Props {
  block: {
    variant?: string;
    label: string;
    url: string;
  };
}

const { block } = Astro.props;
---

<p>
  <a class:list={["btn", `btn--${block.variant}`]} href={block.url}>
    {block.label}
  </a>
</p>
