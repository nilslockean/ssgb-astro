---
import type { EmbedOptions } from "@lib/fientaUtils";

interface Props {
  options: EmbedOptions;
}

const { options } = Astro.props;
---

<script define:vars={{ options }} is:inline>
  window.fientaSettings = options;
</script>
<script is:inline src="https://fienta.com/embed.js"></script>
