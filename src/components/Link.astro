---
import { composePath } from "@lib/routeUtils";
import type { HTMLAttributes } from "astro/types";
import { localeSchema } from "src/schemas/locale";

export interface Props extends HTMLAttributes<"a"> {
  slug?: string;
}

const { slug, href, class: className, ...rest } = Astro.props;
const locale = localeSchema.catch("sv").parse(Astro.currentLocale);
const to = slug ? composePath(slug, locale) : href;
---

<a class:list={["link", className]} href={to} {...rest}><slot /></a>

<style>
  .link {
    text-decoration: underline;
    text-underline-offset: 4px;
    color: var(--color-link);
  }

  .link:hover {
    text-underline-offset: 1px;
    color: var(--color-link-hover);
  }
</style>
