---
import { cn } from "@lib/cn.ts";
import type { HTMLAttributes, HTMLTag } from "astro/types";

type Props<T extends HTMLTag = "p"> = HTMLAttributes<T> & {
  as?: T;
};

const { as, class: className, ...rest } = Astro.props;

// Select wrapper element dynamically
const Tag: HTMLTag = as || "p";
---

<Tag
  {...rest}
  class={cn(
    "font-cooper-hewitt text-accent uppercase tracking-widest text-sm",
    className,
  )}
>
  <slot />
</Tag>
