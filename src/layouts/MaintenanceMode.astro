---
import "../styles/global.css";
import "@fancyapps/ui/dist/fancybox/fancybox.css";
import SiteLogo from "../components/SiteLogo.astro";
import PostHog from "../components/posthog.astro";
import "@fontsource-variable/host-grotesk";
import "@fontsource/instrument-serif";
import "@fontsource/cooper-hewitt/700.css";
import config from "@config";
import { ENABLE_POSTHOG, POSTHOG_PROJECT_API_KEY } from "astro:env/server";

interface Props {
  metaDescription: string;
  metaTitle?: string;
  tagline?: string;
}
const { metaDescription, tagline, metaTitle } = Astro.props;
const url = Astro.url.toString();
const title = metaTitle
  ? `${metaTitle} • ${config.siteTitle}`
  : `${config.siteTitle} • ${tagline || config.siteTagline}`;
---

<!doctype html>
<html lang="sv">
  <head>
    <meta charset="UTF-8" />
    <meta content="width=device-width" name="viewport" />
    <link href="/favicon.svg" rel="icon" type="image/svg+xml" />
    <meta content={Astro.generator} name="generator" />
    <!-- HTML Meta Tags -->
    <title set:html={title} />
    <meta content={metaDescription} name="description" />

    <!-- Facebook Meta Tags -->
    <meta content={url} property="og:url" />
    <meta content="website" property="og:type" />
    <meta content={title} property="og:title" />
    <meta content={metaDescription} property="og:description" />
    <meta content="/og-default.png" property="og:image" />

    <!-- Twitter Meta Tags -->
    <meta content="summary_large_image" name="twitter:card" />
    <meta content="dubbel8.se" property="twitter:domain" />
    <meta content={url} property="twitter:url" />
    <meta content={title} name="twitter:title" />
    <meta content={metaDescription} name="twitter:description" />
    <meta content="/og-default.png" name="twitter:image" />

    {ENABLE_POSTHOG && <PostHog projectApiKey={POSTHOG_PROJECT_API_KEY} />}
    <slot name="head" />
  </head>
  <body class="bg-brand text-muted">
    <div
      class="flex flex-col items-center justify-center gap-8 p-8 pt-16 lg:pt-36"
    >
      <header>
        <SiteLogo displayText={false} height={80} />
      </header>
      <main>
        <slot />
      </main>
    </div>
  </body>
</html>
